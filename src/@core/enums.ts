export enum Field {
  Left = 0,
  Right = 1,
  Middle = 2,
  Home = Left,
  Away = Right,
  Draw = Middle,
  X = Draw,
  Over = Left,
  Under = Right,
}

export enum Network {
  Optimism = 10,
  Arbitrum = 42161,
  Base = 8453,
  OptimismGoerli = 420,
}
